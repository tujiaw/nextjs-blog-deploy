---
title: 'golang vscode launch.json配置'
date: '2020-11-20'
tags: ['others']
draft: false
summary: 文章介绍 golang vscode launch.json 配置，每次调试运行需先打开 main.go 麻烦，增加配置后可直接点击左侧 debug 按钮运行，给出了具体配置内容。 
---

每次调试运行都要先打开main.go然后在菜单里点击Run->Start Debugging或者F5比较麻烦，主要是每次都要定位了main.go文件，这样当我编辑完其他文件后，都要切换一下才能运行。


下面增加一下Launch.json配置就可以直接点击左侧的debug按钮运行了，在Launch菜单下的Add Configuration，内容如下：
```
{
    // Use IntelliSense to learn about possible attributes.
    // Hover to view descriptions of existing attributes.
    // For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
            "env": {},
            "args": []
        }
    ]
}
```